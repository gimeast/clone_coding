import { Footer, Header, SideMenu, TopBanner } from '@/widgets';

const Home = () => {
    return (
        <>
            <TopBanner />
            <Header />
            <SideMenu />
            <Footer />
        </>
    );
};

export default Home;
